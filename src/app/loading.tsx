export default function Loading() {
  return (
    <div
      style={{
        minHeight: '100vh',
        display: 'flex',
        alignItems: 'center',
        justifyContent: 'center',
        background: '#08090a',
        color: '#7e828c',
        fontFamily: 'system-ui, sans-serif',
        fontSize: 14,
      }}
    >
      Loading bracu.network…
    </div>
  );
}
